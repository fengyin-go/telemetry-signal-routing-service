# Telemetry Signal Routing Service

Go service for accepting device signals, routing them through named processing lanes, and returning delivery summaries.

The implementation uses only the standard library and keeps the domain small so the service can be run locally.
